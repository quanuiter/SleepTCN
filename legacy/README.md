# Tài sản cũ — chỉ để đối chiếu

Thư mục này chứa bản sao của notebook/mã đã tạo kết quả trong dự án cũ. Không import trực tiếp từ đây khi chạy thí nghiệm mới.

Mỗi tệp được sao chép phải xuất hiện trong `legacy/SOURCE_MANIFEST.csv` với đường dẫn nguồn, kích thước và mã băm SHA-256.

